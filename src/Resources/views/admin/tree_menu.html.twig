{% set lvl = 0  %}
{% for i,item in items %}
    {% if lvl == item.lvl %}
        </li>
    {% elseif item.lvl > lvl%}
        <ul>
    {% else %}
        </li>
        {% for i in 1..(lvl-item.lvl) %}
            </ul>
            </li>
        {% endfor %}
    {% endif %}
        <li>
            {% include item_template with {item: item} %}
        {% set lvl = item.lvl %}

{% endfor %}

{% for i in 1..lvl %}
        </li>
</ul>
{% endfor%}